var value1 = document.getElementById('event1');
var value2 = document.getElementById('event2');
var resultField = document.getElementById('result');
var form = document.getElementById('form1');



value1.value = "hello";
resultField.innerText = "my";

// numField1.value = 45;
//numField2.value = 46;           //.value is for<input> 
//resulField.innerText = "text";  //this inner.text is to change element //value

// we are write above 3 line to check if our id is connected to html tag //or not  

form.addEventListener('submit', function() {
    // defining function inside function or we can say it is inline //function
    }
    
});